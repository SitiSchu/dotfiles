[module/fs]
type = internal/fs
interval = 120

mount-0 = /

label-mounted = 

warn-percentage = 75
format-warn = <label-warn>
label-warn = D %percentage_used%%
label-warn-foreground = ${colors.alert-yellow}

label-unmounted = %mountpoint% not mounted
label-unmounted-foreground = ${colors.disabled}